#include <opencv2/opencv.hpp>
#include <opencv2/imgproc/imgproc_c.h>

using namespace cv;

/**
 * @brief draw a line, of which expression is pf, on the mat
 *  
 */
void drawCurve(Mat &mat, double (*pf)(double x)) {

}